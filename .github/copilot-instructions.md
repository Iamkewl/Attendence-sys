# Workspace Copilot Instructions

Use instructions from `.github/instructions/`.

Use skills from `.github/skills/` (primary discovery path in VS Code).

`.agents/skills/` remains the source folder and is mirrored into `.github/skills/`.

Primary baseline:
- `.github/instructions/workspace-baseline.instructions.md`
